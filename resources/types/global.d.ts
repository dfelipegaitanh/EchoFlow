// This empty declaration file ensures TypeScript always has at least one input.
// It prevents TS18003 ("No inputs were found") when the project has no .ts/.tsx files yet.
// Safe to keep; it has no runtime impact and no emitted output due to noEmit.
